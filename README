Hello)

It is a multiprocessing proxy checker written on python 3.5+)

It can check all types of proxy (http,socks4,socks5)

It requirements library requests (https://github.com/psf/requests):
  pip install requests
  pip install requests[socks]

Example of running: checker_proxy -proxy=C:\proxy.txt -type_proxy=socks4 -procs=100 -site=google.com

Good luck)
